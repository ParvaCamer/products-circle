{
  "name": "circle-products",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server -w ./data/database.json",
    "dev": "concurrently \"vite\" \"json-server --watch ./data/database.json\"",
    "build": "npm run start & vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "json-server": "^0.17.3",
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.1.0",
    "sass": "^1.62.1",
    "sass-loader": "^13.2.2",
    "vite": "^4.3.2"
  }
}
